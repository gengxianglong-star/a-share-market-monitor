# --- 数据获取引擎 ---
akshare>=1.12.0      # 云端每日增量更新（截面行情 + 板块）
mootdx>=0.11.0       # 本地通达信 vipdoc 日线读取（仅 init_database 使用）

# --- 核心计算与数据处理引擎 ---
pandas>=2.0.0
numpy>=1.24.0

# --- 存储与压缩优化 ---
pyarrow>=14.0.0
fastparquet>=2023.10.0

# --- 辅助工具 ---
tqdm>=4.66.0
requests>=2.31.0
