# Python 缓存
__pycache__/
*.py[cod]
*$py.class

# 虚拟环境
venv/
env/
.env

# 开发工具配置
.vscode/
.idea/

# 注意：data/ 中小文件（JSON、看板等）仍提交 Git；DuckDB 走 GitHub Release
*temp*.csv
*dump*.parquet

# DuckDB 数据库（大文件，由 Release data-latest 托管）
*.duckdb
data/market_monitor.duckdb

# 本地拉取断点缓存（不上传 GitHub）
data/.fetch_cache/
